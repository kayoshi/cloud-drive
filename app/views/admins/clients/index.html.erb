<h1>Listing clients</h1>

<table>
  <tr>
    <th>Firstname</th>
    <th>Lastname</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clients.each do |client| %>
  <tr>
    <td><%= client.firstname %></td>
    <td><%= client.lastname %></td>
    <td><%= link_to 'Show', admins_client_path(client) %></td>
    <td><%= link_to 'Edit', edit_admins_client_path(client) %></td>
    <td><%= link_to 'Destroy', admins_client_path(client), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Client', new_admins_client_path %>
